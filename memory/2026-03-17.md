# 2026-03-17 Durable Memory

- 主人明确要求：禁止“只提醒不执行”。30分钟巡检若无真实动作与结果汇报，等同无效工作。
- 已执行机制调整：删除“每30分钟提醒主人巡检”的cron，改为“静默巡检 + 仅结果汇报（通过项/风险处置）”。
- 主人要求并确认：开发流程固定为“总管派发 -> 开发Agent ->（默认）Codex(ACP)开发 -> 测试Agent验收 -> 总管审核 -> 向主人汇报”。
- 已落地文档：
  - `output/reports/开发任务分发SOP_V1_2026-03-17.md`
  - `output/reports/开发监督反馈机制_V1_2026-03-17.md`
- 开发Agent灵魂文件已固化工作流：`agents/developer/SOUL.md`（开发入口唯一、默认Codex、标准交付包、质量门禁、测试闭环、汇报纪律）。
- ACP能力状态：
  - 已安装并启用 `acpx` 插件；
  - 已配置 `acp.enabled=true`、`acp.backend=acpx`、`defaultAgent=codex`、`allowedAgents`；
  - 已将 `acpx` 加入 `plugins.allow`；
  - 之后 `sessions_spawn(runtime="acp", agentId="codex")` 可被受理。
- 监督经验（关键）：
  - “任务已派发”不等于“已开工”；必须有开工回执（T+2）、过程心跳（T+5）、完工证据（T+10）。
  - Codex会话曾出现无回执超时，必须触发兜底：开发Agent立即接管，不能等待。
- 产品质量经验：
  - 看板路由修复极易出现回归（/board 与 /chat 互串、active状态错误、子路由被过度归一化）。
  - `/board` 仅作为自定义看板总览入口；`/board/*` 必须保留给网关原生非聊天页面，不能再被统一重绘成看板。
  - 验收必须同时覆盖：路由隔离、导航互斥、刷新稳定、子路由可达、聊天入口正确。
- 主人管理偏好（再次确认）：
  - 重结果、重时效、重可审计证据；
  - 不接受“未验证完成”或“口头完成”；
  - 问题应先派发负责人修复，不要把问题本身反复抛给主人。
