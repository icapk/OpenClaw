# Durable Memory — 2026-03-20

## Owner directives (high priority)
- Owner requires strict role separation: XiaoMai must **not** produce analysis/plan/implementation/testing conclusions directly; must dispatch to the corresponding agent and only review/relay approved outputs.
- Repeated concern from owner: forgetting prior instructions and overstepping scope is unacceptable; trust depends on consistent execution, not promises.
- Owner preference: when asked to read links/articles, do it quietly in background; do **not** send screenshots unless explicitly requested.
- Owner prefers direct agent communication (especially with `product` agent) instead of relaying through XiaoMai.

## Stable operating rules added
- Added anti-overreach and pre-reply gate in `MEMORY.md`: analysis/conclusion requests must be delegated first; replies should include source agent + review status when applicable.
- Added anti-duplicate-reply gate in `MEMORY.md`: prevent double sends for same question/runId; aggregate async subagent results into one final response.
- Clarified execution source: `MEMORY.md` is the active rule source; `output/` is archival only.

## Product direction updates from owner
- Current strategic constraint: focus on **C-end only**, avoid B-end heavy-delivery projects.
- Preferred exploration areas: AI education ToC (e.g., English speaking) and AI elderly-care ToC.
- Requested evidence-driven validation from Xiaohongshu before committing to project direction.

## Environment/capability notes
- ACP/Codex invocation is workable via direct `acpx` command path test (`CODEX_OK` observed), while one ACP runtime session path experienced timeout.
- `web_search` (Brave) key not available in current runtime context; browser/manual sourcing used as fallback.

## Routine governance state
- 14:00 and 21:30 overdue audits executed on 2026-03-20: `tasks/in_progress` total 0, overdue 0.
